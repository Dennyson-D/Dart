main() {
  //const lista = const ['Ana', 'Lia', 'Gui'];  //Gera erro quando tenta usar o lista.add, já que é constante nos valores, const na lista não gera erro
  const lista = ['Ana', 'Lia', 'Gui'];
  lista.add('Rebeca');

  print(lista);
}
